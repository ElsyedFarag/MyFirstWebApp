@page
@model RegisterConfirmationModel
@{
    ViewData["Title"] = "تأكيد التسجيل";
    Layout = "";
}

<!DOCTYPE html>
<html lang="ar">
<head>
    <meta charset="utf-8" />
    <title>@ViewData["Title"]</title>
    <link rel="stylesheet" href="~/css/styles.css" />
    <style>
        /* wwwroot/css/styles.css */
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            padding: 0;
        }

        .container {
            max-width: 600px;
            margin: 50px auto;
            padding: 20px;
            background-color: #fff;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            border-radius: 8px;
            text-align: center;
        }

        h1 {
            color: #333;
            font-size: 24px;
            margin-bottom: 20px;
        }

        p {
            font-size: 16px;
            color: #555;
        }

        a#confirm-link {
            display: inline-block;
            margin-top: 20px;
            padding: 10px 20px;
            background-color: #007bff;
            color: #fff;
            text-decoration: none;
            border-radius: 5px;
            transition: background-color 0.3s;
        }

            a#confirm-link:hover {
                background-color: #0056b3;
            }

    </style>
</head>
<body>
    <div class="container">
        <h1>@ViewData["Title"]</h1>
        @if (Model.DisplayConfirmAccountLink)
        {
            <p>
                هذا التطبيق لا يحتوي حاليًا على مرسل بريد إلكتروني حقيقي، انظر <a href="https://aka.ms/aspaccountconf">هذه المستندات</a> لمعرفة كيفية تكوين مرسل بريد إلكتروني حقيقي. عادةً ما يتم إرسال هذا عبر البريد الإلكتروني: <a id="confirm-link" href="@Model.EmailConfirmationUrl">انقر هنا لتأكيد حسابك</a>
            </p>
        }
        else
        {
            <p>
                يرجى التحقق من بريدك الإلكتروني لتأكيد حسابك.
            </p>
        }
    </div>
</body>
</html>
